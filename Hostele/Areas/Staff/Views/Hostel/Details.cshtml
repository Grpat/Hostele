@model Hostele.Models.ViewModels.HostelItemDetailsViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Hostel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Nazwa)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Nazwa)
        </dd>
        
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Opis)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Opis)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Pojemnosc)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Pojemnosc)
        </dd>
    </dl>
    @if (Model!.Pokoje.Any())
         {
    <dt class="col-sm-2">
        Lista pokojów
    </dt>
     
         <select class="form-control col-sm-6">
             @foreach (var item in Model?.Pokoje!)
             {
                 <option> @Html.DisplayFor(modelItem => item.Nazwa)</option>
             }
         </select>
     }
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
